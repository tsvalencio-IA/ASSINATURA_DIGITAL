{
  "name": "assinador-digital-thiaguinho-android",
  "version": "1.1.0",
  "description": "Assinador digital local offline com prévia PDF nativa no Android e APK via GitHub Actions.",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "scripts": {
    "prepare-www": "node scripts/prepare-www.mjs",
    "android:add": "npx cap add android",
    "android:sync": "npm run prepare-www && npx cap sync android",
    "android:apk": "npm run android:sync && npm run android:native && cd android && ./gradlew assembleDebug --no-daemon",
    "android:native": "node scripts/install-android-native.mjs"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/core": "latest",
    "@capacitor/filesystem": "latest",
    "@capacitor/share": "latest"
  }
}
